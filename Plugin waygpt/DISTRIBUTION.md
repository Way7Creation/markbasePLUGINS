# Безопасность распространения

## ✅ Можно отправлять любому проекту

Папка **Plugin waygpt** предназначена для распространения. Её можно отправлять сторонним разработчикам, партнёрам или подключать в любые проекты.

---

## 🔒 Что внутри

- **Только код и документация** — SDK (Python, JavaScript, PHP), примеры, инструкции.
- **Нет конфиденциальных данных** — никаких реальных API-ключей, секретов, паролей.
- **Только заглушки в примерах** — `sk_live_...`, `your-project-id`, `your-hmac-secret`, `xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx`. Реальные значения разработчик подставляет сам в своём окружении.
- **Нет привязки к вашему серверу** — используется лишь публичный URL `https://app.waygpt.ru` как значение по умолчанию (аналогично `api.openai.com` в документации OpenAI). В коде и инструкциях везде указано «или ваш домен» — получатель может указать свой AI Server.
- **Нет .env и секретов** — файлы `.env` в `.gitignore`, в репозитории их нет. Ключи хранятся только у того, кто использует плагин.

---

## 📦 Что получает разработчик

- Готовый плагин для **удалённого** управления возможностями AI Server: чат, генерация изображений и видео, сценарии, виджет и т.д.
- Документацию по установке, настройке и использованию.
- Примеры на Python, JavaScript и PHP.
- Всё необходимое, чтобы подключить свой проект к AI Server (вашему или своему) и начать разработку.

---

## ⚠️ Важно для получателя

Ключи и секреты (**Project Key**, **Project ID**, **HMAC Secret**) получатель создаёт сам в кабинете AI Server и передаёт в плагин через переменные окружения или конфиг. В распространённой папке их нет и быть не должно.

---

**Итог:** папку можно смело передавать любым проектам. Данных для доступа к вашим системам в ней нет, только рабочий код и инструкции для разработки.
